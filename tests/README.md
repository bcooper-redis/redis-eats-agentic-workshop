# Tests

This directory is reserved for automated tests.

For Workshop 2, manual validation is performed by running the notebook top-to-bottom
and verifying each checkpoint cell passes with live Redis Cloud, Agent Memory,
Context Retriever, and OpenAI credentials.

Automated tests covering connectivity, tool execution, agent memory operations,
and end-to-end agent responses may be added in a future iteration.
