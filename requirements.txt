# Redis Eats Agentic Workshop — Python dependencies
# Install with: pip install -r requirements.txt

# Redis
redis>=5.0.0
redisvl>=0.20.0

# Redis Iris Context Engine
redis-agent-memory>=0.0.4         # Agent Memory (session + long-term)
context-surfaces>=0.0.5           # Context Retriever / Context Surfaces
langcache>=0.12.0                 # Semantic caching

# OpenAI
openai>=1.30.0

# PDF parsing (for W1 policy index reload)
pypdf>=4.0.0

# Sentence Transformers (used by RedisVL SemanticRouter)
sentence-transformers>=2.2.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0
